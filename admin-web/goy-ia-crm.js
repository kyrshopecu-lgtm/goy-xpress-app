(function(){'use strict';
const PIPELINE=['Nuevo','Contactado','Respondió','Calificado','Interesado','Reunión agendada','Cliente','No interesado'];
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function card(label,value){return `<div class="goy-ia-stat"><strong>${esc(value)}</strong><span>${esc(label)}</span></div>`;}
function render(root,data={}){if(!root)return;const prospects=Array.isArray(data.prospects)?data.prospects:[];const appointments=Array.isArray(data.salesAppointments)?data.salesAppointments:[];root.innerHTML=`<section class="goy-ia"><header><div><small>ASISTENTE COMERCIAL</small><h2>GOY IA</h2><p>Prospectos, conversaciones y reuniones en un solo lugar.</p></div><span class="goy-ia-live">● Activo</span></header><div class="goy-ia-stats">${card('Prospectos',prospects.length)}${card('Interesados',prospects.filter(p=>p.status==='Interesado').length)}${card('Reuniones',appointments.filter(a=>a.status==='scheduled').length)}${card('Clientes',prospects.filter(p=>p.status==='Cliente').length)}</div><div class="goy-ia-pipeline">${PIPELINE.map(status=>`<article><h3>${esc(status)} <b>${prospects.filter(p=>p.status===status).length}</b></h3>${prospects.filter(p=>p.status===status).slice(0,12).map(p=>`<button type="button" data-prospect="${esc(p.id)}"><strong>${esc(p.name)}</strong><span>${esc(p.businessName||p.city||p.phone)}</span><small>${esc(p.serviceInterest?.name||'Por calificar')} · ${Number(p.score||0)}%</small></button>`).join('')||'<p class="empty">Sin prospectos</p>'}</article>`).join('')}</div></section>`;}
window.GoyIaCrm={render,PIPELINE};
})();